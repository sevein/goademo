// Code generated by goa v3.2.2, DO NOT EDIT.
//
// goademo HTTP client CLI support package
//
// Command:
// $ goa gen github.com/sevein/goademo/design -o api

package cli

import (
	"flag"
	"fmt"
	"net/http"
	"os"

	democ "github.com/sevein/goademo/api/gen/http/demo/client"
	goahttp "goa.design/goa/v3/http"
	goa "goa.design/goa/v3/pkg"
)

// UsageCommands returns the set of commands and sub-commands using the format
//
//    command (subcommand1|subcommand2|...)
//
func UsageCommands() string {
	return `demo (show|list)
`
}

// UsageExamples produces an example of a valid invocation of the CLI tool.
func UsageExamples() string {
	return os.Args[0] + ` demo show --id 17449060764402106671` + "\n" +
		""
}

// ParseEndpoint returns the endpoint and payload as specified on the command
// line.
func ParseEndpoint(
	scheme, host string,
	doer goahttp.Doer,
	enc func(*http.Request) goahttp.Encoder,
	dec func(*http.Response) goahttp.Decoder,
	restore bool,
) (goa.Endpoint, interface{}, error) {
	var (
		demoFlags = flag.NewFlagSet("demo", flag.ContinueOnError)

		demoShowFlags  = flag.NewFlagSet("show", flag.ExitOnError)
		demoShowIDFlag = demoShowFlags.String("id", "REQUIRED", "")

		demoListFlags    = flag.NewFlagSet("list", flag.ExitOnError)
		demoListNameFlag = demoListFlags.String("name", "", "")
	)
	demoFlags.Usage = demoUsage
	demoShowFlags.Usage = demoShowUsage
	demoListFlags.Usage = demoListUsage

	if err := flag.CommandLine.Parse(os.Args[1:]); err != nil {
		return nil, nil, err
	}

	if flag.NArg() < 2 { // two non flag args are required: SERVICE and ENDPOINT (aka COMMAND)
		return nil, nil, fmt.Errorf("not enough arguments")
	}

	var (
		svcn string
		svcf *flag.FlagSet
	)
	{
		svcn = flag.Arg(0)
		switch svcn {
		case "demo":
			svcf = demoFlags
		default:
			return nil, nil, fmt.Errorf("unknown service %q", svcn)
		}
	}
	if err := svcf.Parse(flag.Args()[1:]); err != nil {
		return nil, nil, err
	}

	var (
		epn string
		epf *flag.FlagSet
	)
	{
		epn = svcf.Arg(0)
		switch svcn {
		case "demo":
			switch epn {
			case "show":
				epf = demoShowFlags

			case "list":
				epf = demoListFlags

			}

		}
	}
	if epf == nil {
		return nil, nil, fmt.Errorf("unknown %q endpoint %q", svcn, epn)
	}

	// Parse endpoint flags if any
	if svcf.NArg() > 1 {
		if err := epf.Parse(svcf.Args()[1:]); err != nil {
			return nil, nil, err
		}
	}

	var (
		data     interface{}
		endpoint goa.Endpoint
		err      error
	)
	{
		switch svcn {
		case "demo":
			c := democ.NewClient(scheme, host, doer, enc, dec, restore)
			switch epn {
			case "show":
				endpoint = c.Show()
				data, err = democ.BuildShowPayload(*demoShowIDFlag)
			case "list":
				endpoint = c.List()
				data, err = democ.BuildListPayload(*demoListNameFlag)
			}
		}
	}
	if err != nil {
		return nil, nil, err
	}

	return endpoint, data, nil
}

// demoUsage displays the usage of the demo command and its subcommands.
func demoUsage() {
	fmt.Fprintf(os.Stderr, `Service is the demo service interface.
Usage:
    %s [globalflags] demo COMMAND [flags]

COMMAND:
    show: Show implements show.
    list: List implements list.

Additional help:
    %s demo COMMAND --help
`, os.Args[0], os.Args[0])
}
func demoShowUsage() {
	fmt.Fprintf(os.Stderr, `%s [flags] demo show -id UINT

Show implements show.
    -id UINT: 

Example:
    `+os.Args[0]+` demo show --id 17449060764402106671
`, os.Args[0])
}

func demoListUsage() {
	fmt.Fprintf(os.Stderr, `%s [flags] demo list -name STRING

List implements list.
    -name STRING: 

Example:
    `+os.Args[0]+` demo list --name "Repudiandae minima maxime."
`, os.Args[0])
}
